[project]
name = "cms"
version = "0.1.0"
description = "Local editor and static-site generator for A4 HTML collages"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "jinja2>=3.1.6",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
